//! `superx` — the operator CLI for the SuperX agentic OS.
//!
//! The command surface (`boot`, `status`, `agents`, `stats --live`)
//! lands in G6 per `BLUEPRINT.md`. Until then this binary only
//! identifies itself.

fn main() {
    println!("superx 0.2.0 — agentic-OS reset in progress (G0); see BLUEPRINT.md");
}
