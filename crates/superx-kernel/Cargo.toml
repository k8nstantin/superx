[package]
name = "superx-kernel"
description = "SuperX agentic-OS kernel: substrate verbs over SurrealDB, boot, the agent-telemetry capture engine, and the module system"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
