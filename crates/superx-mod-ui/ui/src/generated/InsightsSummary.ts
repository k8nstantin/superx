// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AgentSplit } from "./AgentSplit";
import type { HeatCell } from "./HeatCell";
import type { ModuleHealth } from "./ModuleHealth";
import type { NameCount } from "./NameCount";
import type { TableStat } from "./TableStat";
import type { TimeCount } from "./TimeCount";
import type { TokenTotals } from "./TokenTotals";

/**
 * Deep statistics (issue #237) — engine-side aggregates over ALL
 * history, not a window. Served separately from [`StatsSummary`] so
 * the live tiles keep their fast refresh.
 */
export type InsightsSummary = { 
/**
 * Events per calendar day, oldest first (`t` = `YYYY-MM-DD`).
 */
events_per_day: Array<TimeCount>, 
/**
 * Events by hour of day × day of week.
 */
hour_weekday: Array<HeatCell>, tokens: TokenTotals, 
/**
 * Messages by model name, descending.
 */
models: Array<NameCount>, 
/**
 * Messages and output tokens by agent, descending.
 */
per_agent: Array<AgentSplit>, 
/**
 * Telemetry volume by event kind, descending.
 */
event_kinds: Array<NameCount>, 
/**
 * Newest startup reading per module, in milliseconds.
 */
module_startup: Array<NameCount>, 
/**
 * Age of the newest captured event — the capture-alive signal.
 * An age computed on the server goes stale the moment it is sent:
 * this panel refreshes once a minute, the flight deck every
 * fifteen seconds, and a cached answer freezes it further, so two
 * panels showed two different lags for the same instance (#400).
 * The timestamp below is the truth; the age is what it was when
 * the answer was built, kept for anything that wants the server's
 * own view.
 */
last_event_secs: bigint | null, 
/**
 * When the newest captured event happened, RFC3339. The page
 * counts up from this, every second, so every age on it is the
 * same age.
 */
last_event_at: string | null, events_last_hour: bigint, 
/**
 * Per-module health from the lifecycle stream (#367): the
 * substrate held every `module_failed` and nothing read them.
 */
module_health: Array<ModuleHealth>, 
/**
 * Every table in the substrate, biggest first (#398).
 */
tables: Array<TableStat>, 
/**
 * Rows across all of them, and what they weigh, estimated.
 */
db_rows_total: bigint, db_bytes_est: bigint, };
